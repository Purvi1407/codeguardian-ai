/**
 * TS-typed parameter variants — required, optional, and with defaults —
 * to exercise the required_parameter/optional_parameter branch of
 * _param_name in js_ts_parser.py.
 */
export function typedParams(a: string, b?: number, c: boolean = true): void {
  return;
}
